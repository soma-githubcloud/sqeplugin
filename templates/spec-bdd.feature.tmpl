# Generated by sqe-kit | Kit: {{kitId}} | {{generatedDate}}
# Source test cases: {{testCaseIds}}
# Regenerate with: /sqe-kit:generate-automation-tests --style bdd

Feature: {{featureName}}

  {{#each scenarios}}
  {{#each this.tags}}@{{this}} {{/each}}
  Scenario: {{this.id}} - {{this.title}}
    {{#each this.given}}
    Given {{this}}
    {{/each}}
    {{#each this.when}}
    {{#if @first}}When {{else}}And  {{/if}}{{this}}
    {{/each}}
    {{#each this.then}}
    {{#if @first}}Then {{else}}And  {{/if}}{{this}}
    {{/each}}

  {{/each}}
